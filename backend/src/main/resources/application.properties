# Application Configuration
spring.application.name=CareerCoach

# Server Configuration
server.port=8080

# Database Configuration
spring.data.mongodb.uri=mongodb+srv://niazrahman:Nahin081210@cluster0.czpzwu9.mongodb.net/CareerCoach?retryWrites=true&w=majority&appName=Cluster0

# Spring Security Configuration
spring.security.debug=false

# Logging Configuration (for debugging)
logging.level.org.springframework.security=INFO
logging.level.org.springframework.web=DEBUG
logging.level.com.careercoach=DEBUG
logging.level.com.careercoach.service.CVReviewService=DEBUG
logging.level.com.careercoach.controllers.CVReviewController=DEBUG
logging.level.com.careercoach.service.NotificationEventPublisher=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.org.mongodb.driver=DEBUG

# File Upload Configuration
app.upload.dir=uploads
app.upload.max-file-size=5242880
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# CORS Configuration (handled in Java config)
# Allow all origins during development
management.endpoints.web.cors.allowed-origins=http://localhost:5173,http://localhost:3000,http://localhost:5174
management.endpoints.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
management.endpoints.web.cors.allowed-headers=*
management.endpoints.web.cors.allow-credentials=true